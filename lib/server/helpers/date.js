const moment = require('moment-timezone');

/*
static String dateAndTimeFormat(DateTime value) {
    return DateFormat('date-and-time-format'.tr).format(value);
}

static String dateFormat(DateTime value) {
    return DateFormat('date-format'.tr).format(value);
}
*/

function dateFormat(value, format) {

}
